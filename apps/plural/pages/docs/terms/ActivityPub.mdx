import { DocLayout } from "@plural/ui";

# ActivityPub

ActivityPub defines a common protocol that is used by most servers on the
[Fediverse](/docs/terms/fediverse/) to communicate.

export default ({ children }) => <DocLayout term="ActivityPub">{ children }</DocLayout>;
